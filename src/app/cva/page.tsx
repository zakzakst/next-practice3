import { Button } from "./components/button";

const Page = () => {
  return (
    <div>
      <Button variant='default' size='lg'>button</Button>
      <Button variant='default' size='lg' loading>button</Button>
    </div>
  )
}

export default Page